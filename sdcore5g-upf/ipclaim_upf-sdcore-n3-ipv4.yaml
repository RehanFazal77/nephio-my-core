apiVersion: ipam.resource.nephio.org/v1alpha1
kind: IPClaim
metadata:
  name: upf-sdcore-n3-ipv4
  annotations:
    config.kubernetes.io/local-config: "true"
    specializer.nephio.org/for: workload.nephio.org/v1alpha1.NFDeployment.upf-sdcore
    specializer.nephio.org/owner: req.nephio.org/v1alpha1.Interface.n3
spec:
  kind: network
  selector:
    matchLabels:
      nephio.org/address-family: ipv4
      nephio.org/cluster-name: sdcore
      nephio.org/network-name: n3
  networkInstance:
    name: vpc-ran
status: {}
